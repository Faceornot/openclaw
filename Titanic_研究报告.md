# 《泰坦尼克号幸存者预测》竞赛研究报告

## 1. 研究背景

《Titanic - Machine Learning from Disaster》是 Kaggle 平台上的经典入门竞赛，其目标是根据乘客的基础信息预测其是否幸存。该任务本质上是一个二分类问题，适合用于练习数据预处理、探索性数据分析、特征工程、模型构建、模型评估与结果解释等完整的数据挖掘流程。

与视频中的基础流程相比，本研究重点从数据分析、缺失值处理、特征工程、模型改进和评估方式等方面进行了优化，以期获得更稳定、更准确的预测结果。

---

## 2. 数据分析有哪些可以优化，如何实现？

### 2.1 缺失值处理可以进一步优化

视频中的基础方法通常采用整体均值、中位数或众数进行缺失值填补，这种方法实现简单，但忽略了不同群体之间的数据分布差异。

本研究中的优化方案如下：

- 对 `Age` 不再直接整体填补，而是结合 `Title`（称谓）与 `Pclass`（舱位等级）进行分组中位数填补。
- 对 `Fare` 按 `Pclass` 分组进行中位数填补。
- 对 `Embarked` 使用众数填补。
- 对 `Cabin` 不直接删除，而是提取是否有舱位信息以及舱位首字母等衍生特征。

**实现原因：**
不同舱位、不同称谓、不同社会身份乘客的年龄与票价分布存在明显差异，分组填补比整体填补更符合实际情况，从而有助于提升模型的判别能力。

### 2.2 探索性分析维度可以更加全面

基础分析往往只关注 `Sex` 和 `Pclass` 与生存率的关系，但实际上还可以进一步分析下列变量：

- `Age` 与生存率的关系
- `Fare` 与生存率的关系
- `Embarked` 与生存率的关系
- `FamilySize` 与生存率的关系
- `Title` 与生存率的关系
- `CabinKnown` 与生存率的关系

**实现方式：**
可通过柱状图、箱线图、分组统计表等方式观察不同特征与生存率之间的差异，从而指导后续特征工程和模型选择。

### 2.3 特征工程可以从“原始字段”提升到“信息提炼”

相比直接使用原始字段，本研究增加了如下衍生特征：

1. **Title**：从 `Name` 中提取称谓，例如 `Mr`、`Mrs`、`Miss`、`Master` 等，并将低频称谓合并为 `Rare`。
2. **FamilySize**：由 `SibSp + Parch + 1` 得到，反映乘客同行家庭规模。
3. **IsAlone**：用于表示乘客是否独自出行。
4. **CabinKnown**：表示是否存在舱位记录。
5. **CabinLetter**：提取舱位编号首字母，反映客舱区域信息。

**优化意义：**
这些特征比原始变量更具解释性，更容易反映乘客身份、家庭结构和舱位条件等隐含信息，对生存预测有明显帮助。

### 2.4 数据预处理流程可以标准化

为了避免训练集和测试集处理方式不一致，本研究采用 `Pipeline` 与 `ColumnTransformer` 构建统一预处理流程，对数值特征与类别特征分别处理。

- 数值特征：缺失值填补 + 标准化
- 类别特征：缺失值填补 + 独热编码

**优点：**
- 提高代码规范性和可复现性
- 避免数据泄漏
- 方便后续替换模型和做交叉验证

---

## 3. 数据挖掘模型如何改进，原因？

### 3.1 基础模型的局限性

视频中的基础模型通常包括逻辑回归、决策树或简单随机森林。

其中，逻辑回归的优点是结构简单、可解释性较强、训练速度快，但它更适合线性可分问题，对于特征之间复杂的非线性关系和交互作用捕捉能力有限。

### 3.2 使用更强的集成模型

本研究将基础模型改进为 **梯度提升树模型（Gradient Boosting）**，原因如下：

- 能够更好地处理表格型结构化数据
- 能自动学习非线性关系
- 能捕捉特征间的交互作用
- 在 Titanic 这类中小规模数据集上通常优于简单线性模型

与单一模型相比，提升类模型往往有更强的泛化能力，因此更适合用于 Kaggle 竞赛中的性能优化。

### 3.3 引入交叉验证进行稳定评估

如果只采用一次训练集/验证集划分，模型结果容易受随机划分影响，导致评估不稳定。

因此，本研究采用 **5 折交叉验证** 对模型进行评估，计算平均准确率作为模型性能指标。这样可以使结果更稳定，更能反映模型的真实泛化能力。

### 3.4 模型改进的原因总结

模型改进的核心原因包括：

- 基础模型表达能力有限
- Titanic 数据存在明显的非线性关系
- 表格数据任务中，集成模型通常表现更优
- 交叉验证能减少偶然性，提高结论可信度

---

## 4. 模型结果分析说明

### 4.1 关键特征影响分析

结合数据分析与模型训练结果，可以看出以下特征对幸存预测影响较大：

- **Sex**：女性生存率明显高于男性，是最关键的预测特征之一。
- **Pclass**：一等舱乘客通常享有更好的逃生条件，因此生存率更高。
- **Age**：儿童群体的生存率相对较高。
- **Fare**：票价较高的乘客通常位于较高舱位，生存概率相对更高。
- **Title**：称谓可反映年龄、性别和社会身份，例如 `Master` 多为儿童，`Mrs` 多为成年女性，这些信息有助于区分生存概率。
- **FamilySize / IsAlone**：家庭规模也会对生存产生影响，过大或过小的同行规模都可能影响逃生机会。

### 4.2 改进模型与基础模型对比分析

与逻辑回归等基础模型相比，改进后的梯度提升模型能够学习更复杂的特征关系，因此在交叉验证和竞赛提交结果中通常表现更好。

对比可以总结为：

- 逻辑回归：稳定、简单、易解释，但对复杂模式捕捉不足
- 随机森林：能处理非线性关系，鲁棒性较强
- 梯度提升模型：在结构化数据上通常有更好的预测性能

因此，本研究最终采用了经过特征工程与预处理优化后的提升类模型作为提交模型。

### 4.3 模型误差来源分析

尽管模型性能有所提升，但仍然存在一定误差，主要原因如下：

- 样本规模有限，训练数据只有数百条
- 某些特征缺失严重，例如 `Cabin`
- 数据中未记录所有真实影响幸存的因素
- 测试集与训练集分布可能存在轻微差异
- 部分特征之间关系复杂，单一模型仍难以完全刻画

---

## 5. Kaggle 提交与排名结果

将优化后的预测结果提交到 Kaggle Titanic 竞赛平台后，可在 leaderboard 页面查看本次提交得分与排名。

### 5.1 需要补充的信息

- **Kaggle Score：**（提交后填写）
- **Leaderboard 排名：**（提交后填写）

### 5.2 排名截图

请在此处插入 leaderboard 截图。

> 截图要求：
> - 能清楚显示比赛名称
> - 能清楚显示本次提交得分
> - 能清楚显示排名或相对位置

---

## 6. 总结

本研究在视频讲解的基础流程之上，对 Titanic 幸存者预测任务进行了进一步优化，主要体现在以下几个方面：

1. 对缺失值处理进行了更细致的分组填补；
2. 构造了 `Title`、`FamilySize`、`IsAlone`、`CabinKnown`、`CabinLetter` 等更有信息量的特征；
3. 使用 `Pipeline` 和 `ColumnTransformer` 统一预处理过程，提高了流程规范性；
4. 采用梯度提升模型替代基础模型，增强了模型对非线性关系的学习能力；
5. 使用交叉验证对模型进行更稳定的评估。

综合来看，本次优化说明在数据挖掘任务中，除了模型选择之外，数据理解、缺失值处理和特征工程同样是影响最终效果的重要因素。通过合理优化这些环节，可以显著提升模型表现，并在 Kaggle 竞赛中获得更好的成绩。

---

## 7. 后续补充建议

当你完成 Kaggle 提交后，建议把以下内容补入最终版报告：

- leaderboard 截图
- 最终得分
- 最终排名
- 与视频基础模型结果的分数对比
- 自己的实验心得或改进体会
