# 2026-03-26

- 用户要求转向具身智能中的 VLM/VLA 方向，不再继续 Go2 locomotion；接受仿真生成数据、轨迹数据、benchmark 作为数据资源。
- 最终收敛的主线：基于 Isaac Sim / IsaacLab 的 Franka 机械臂 manipulation，先做基础 lift/reach baseline，再扩展为“多物体 + 语言条件 + oracle target”的具身任务，后续再接 VLM/VLA。
- 远程服务器已确认并打通：`10.168.1.102:22`，用户 `yhr`。最初密码登录存在兼容问题，后改为添加本机 SSH 公钥后成功免密登录。
- 服务器关键信息：主机名 `ds`，路径 `/home/yhr`，GPU 为 7 张 RTX 2080 Ti；IsaacLab 位于 `/home/yhr/IsaacLab`；正确 conda 环境为 `/d/miniconda3/envs/env_isaaclab`（Python 3.11.14）。
- 已确认 IsaacLab 中 Franka manipulation 环境可用，成功列出并验证包括：`Isaac-Reach-Franka-v0`、`Isaac-Lift-Cube-Franka-v0`、`Isaac-Stack-Cube-Franka-v0` 等。
- 已实际完成的运行验证：
  - `Isaac-Reach-Franka-v0` 最小训练 smoke test 成功。
  - `Isaac-Lift-Cube-Franka-v0` 最小训练 smoke test 成功。
  - `baseline50` 成功完成，产出目录：`/home/yhr/IsaacLab/logs/rsl_rl/franka_lift/2026-03-26_11-08-53_baseline50`，包含 `model_0.pt`、`model_49.pt`、tensorboard 日志和参数文件。
  - `baseline300` 成功完成，产出目录：`/home/yhr/IsaacLab/logs/rsl_rl/franka_lift/2026-03-26_11-29-08_baseline300`，包含 `model_0.pt`、`model_50.pt`、`model_100.pt`、`model_150.pt`、`model_200.pt`、`model_250.pt`、`model_299.pt`、tensorboard 日志和参数文件。
- `baseline300` 的 `model_299.pt` 已验证为可用 checkpoint：
  - 用于 `play.py` 时，第一次因相对路径错误失败；第二次使用绝对路径后确认 checkpoint 能成功加载。
  - 但 `play/eval` 过程在 Omniverse / carb 底层触发 `Mutex.cpp` / `Recursion not allowed` 断言并退出。当前判断：训练链路稳定，play/eval 路线在该机上不够稳定。
- 已发起并成功接上的续训任务：
  - 任务名：`resume1000`
  - 基于 `baseline300` 的 `model_299.pt` 恢复训练
  - 输出目录：`/home/yhr/IsaacLab/logs/rsl_rl/franka_lift/2026-03-26_12-08-54_resume1000`
  - 标准输出日志：`/home/yhr/IsaacLab/logs/rsl_rl/franka_lift/resume1000_stdout.log`
  - 观测到续训时已进入 `Learning iteration 318/1299` 及之后的迭代，说明 resume 链路成功。
- 已完成的研究设计收敛：
  - 项目本质：不是单纯训练一个 Franka lift policy，而是要把现有单目标 lift 任务升级成“多物体 + 语言条件 + oracle target”的具身任务，再逐步接入真实 VLM/VLA。
  - 当前最重要的下一阶段不是直接接大模型，而是实现 `multi-object + language-conditioned oracle baseline`。
- 已明确的创新点归纳：
  1. 将 IsaacLab 现有单目标 Franka lift 扩展为多物体语言条件 manipulation 任务。
  2. 生成多物体场景、语言指令、目标标签、轨迹数据，形成仿真数据集与 benchmark 雏形。
  3. 采用从 oracle baseline 到真实 VLM/VLA 的渐进式路线，兼顾落地性与研究扩展性。
- 已完成对现有 lift 代码结构的梳理：
  - `joint_pos_env_cfg.py`：定义 Franka 机器人、单个 `scene.object` 以及 EE frame。
  - `lift_env_cfg.py`：定义通用 scene / observations / events / rewards / terminations。
  - `mdp/observations.py`、`mdp/rewards.py`、`mdp/terminations.py`：当前都强绑定单个 `object`，默认只服务单目标任务。
- 已明确的工程改造方向：
  - 不能只在 scene 中多放几个物体；必须同步改 observation、reward、termination、reset event。
  - 第一版要新建独立任务配置和 agent 配置，而不是直接污染现有 `Lift-Cube-Franka`。
  - 真正的核心实现难点是：在 env/reset 时维护 `target_object_id` 等 oracle metadata，并让 reward 按动态 target object 计算。
- 用户明确要求：这些进程和当前进度很重要，需要持久化记住，下次继续时直接沿着本进度推进。
- 下次继续时的建议起点：
  1. 先检查 `resume1000` 是否还在运行、是否新增 checkpoint；
  2. 再继续查看现有 stack/manipulation 任务对多对象和复杂 observation 的实现，作为多物体 oracle baseline 的参考；
  3. 然后开始实际新建 `multi_object_oracle` 任务配置与代码改造。
