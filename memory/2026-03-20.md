# 2026-03-20

- User provided durable operating rules to store in memory, including mandatory changelog maintenance, timestamp-prefixed replies, gateway restart notification, step-by-step progress reports, immediate git commit+push for each change, no file deletion, and todo tracking requirements.
- The user explicitly asked that each subsequent step and relevant ongoing context be remembered continuously until they say it is no longer needed.
- Recorded Isaac Lab multi-GPU training findings provided by the user: environment and Go2 task are healthy; 1-card training on cuda:3 succeeded with model artifacts; 4-card training also succeeded with saved checkpoints; 7-card distributed runs were unstable and likely caused host disconnect/reboot; current practical recommendation is to use 4 GPUs for stable training and probe 5-6 GPUs before retrying 7.
- Started a new 4-GPU Go2 baseline run on host `10.168.1.102` using `env_isaaclab`, task `Isaac-Velocity-Flat-Unitree-Go2-v0`, `torch.distributed.run --nproc_per_node=4`, and `--max_iterations 100`. Manual log file: `/home/yhr/IsaacLab/logs/manual_runs/go2_flat_4gpu_baseline_2026-03-20_08-02-25.log`. Confirmed the run entered training iterations and progressed at least to iteration 9/100.
- Later confirmed that the same 4-GPU Go2 baseline completed successfully through iteration 99/100 and exited cleanly. Output directory: `/home/yhr/IsaacLab/logs/rsl_rl/unitree_go2_flat/2026-03-20_08-02-41`, with artifacts `events.out.tfevents...`, `model_0.pt`, `model_50.pt`, `model_99.pt`, `params/agent.yaml`, and `params/env.yaml`.
- Per user request, started a new 4-GPU rough-terrain Go2 training run on host `10.168.1.102` using task `Isaac-Velocity-Rough-Unitree-Go2-v0`, `env_isaaclab`, and `--max_iterations 100`. Manual log file: `/home/yhr/IsaacLab/logs/manual_runs/go2_rough_4gpu_baseline_2026-03-20_11-45-34.log`. Confirmed successful startup through terrain generation and environment initialization.
