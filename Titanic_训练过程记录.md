# Titanic 训练过程记录

## 一、任务来源

这一阶段的训练/建模工作围绕 Kaggle 的 Titanic（泰坦尼克号幸存者预测）任务展开。目标不是只跑通一个基础模型，而是在原有视频讲解流程的基础上完成以下几件事：

- 优化原始数据分析流程；
- 改进基础模型；
- 生成可提交到 Kaggle 的预测结果；
- 记录模型对比结果；
- 最终形成研究报告与可提交文档。

## 二、最初阶段：明确交付目标

在最开始，这次训练被明确为一个完整的数据挖掘作业，而不是简单跑代码。整理出的核心交付物包括：

1. 优化后的 Titanic 建模代码；
2. 一份研究报告；
3. 可用于 Word 打开的文档版本；
4. Kaggle 提交成绩与排名信息；
5. 对分析流程和模型改进原因的说明。

也就是说，这次训练从一开始就包含“数据分析 + 建模 + 结果说明 + 文档整理”四个部分。

## 三、数据处理阶段：对基础流程做优化

相对于视频中的基础流程，这一阶段没有停留在“缺失值简单填补 + 类别编码 + 训练一个基础模型”的层面，而是做了更系统的预处理设计。

### 1. 缺失值处理优化

主要做法如下：

- 对 `Age` 按 `Title`（称谓）和 `Pclass`（舱位等级）分组后用中位数填补；
- 对 `Fare` 按 `Pclass` 分组后用中位数填补；
- 对 `Embarked` 使用众数填补；
- 对 `Cabin` 不直接丢弃，而是提取衍生信息。

这一步的核心思路是：不同人群的年龄和票价分布不同，不能简单粗暴地用全局均值或中位数统一填补。

### 2. 特征工程构造

训练过程中额外构造了多个特征：

- `Title`：从姓名里提取称谓，如 `Mr`、`Mrs`、`Miss`、`Master`；
- `FamilySize`：家庭成员规模，计算方式为 `SibSp + Parch + 1`；
- `IsAlone`：是否独自出行；
- `CabinKnown`：是否存在舱位信息；
- `CabinLetter`：舱位首字母；

这一阶段的重点不是“多造几个字段”，而是让模型能学到更有解释力的信息，例如身份特征、家庭结构和舱位条件。

### 3. 预处理流程标准化

为了避免训练集和测试集处理不一致，整个训练流程中引入了：

- `Pipeline`
- `ColumnTransformer`

其中：

- 数值特征做：缺失值填补 + 标准化；
- 类别特征做：缺失值填补 + One-Hot 编码。

这一阶段让训练流程变得更规范，也方便后面比较多个模型。

## 四、模型训练阶段：多模型比较

这次训练没有只用一个模型，而是进行了多模型对比。

参与比较的模型包括：

- `LogisticRegression`
- `RandomForestClassifier`
- `GradientBoostingClassifier`

评估方式采用了 **5 折交叉验证**，而不是只做一次简单划分。这样可以减少偶然性，使模型对比更稳定。

### 交叉验证结果

训练过程中得到的平均准确率如下：

- LogisticRegression：`0.83166`
- RandomForest：`0.82940`
- GradientBoosting：`0.82044`

### 训练结论

最终表现最好的模型是：

- **LogisticRegression**

虽然逻辑回归是相对简单的模型，但在当前这套特征工程和数据规模下，它的交叉验证结果优于随机森林和梯度提升模型。因此，最终使用逻辑回归模型拟合训练集，并生成 Kaggle 提交文件 `submission.csv`。

## 五、结果生成阶段：输出提交文件

在确定最优模型后，训练流程继续完成了以下动作：

1. 用最佳模型在完整训练集上重新拟合；
2. 对测试集进行预测；
3. 生成提交文件：`submission.csv`；
4. 让该文件可以直接用于 Kaggle 平台提交。

这意味着训练过程不是停留在本地评估，而是完整走到了可提交阶段。

## 六、文档整理阶段：把训练结果写成正式材料

这段训练之后，又进一步把成果整理成了多个文档版本：

1. `Titanic_研究报告.md`
   - 正式的课程/竞赛风格研究报告；
   - 包含实验背景、数据分析优化、模型改进、结果分析、总结等内容；

2. `Titanic_研究报告.rtf`
   - Word 兼容版本；

3. `Titanic_研究报告_可用版.html`
   - 因为 RTF 打开存在乱码风险，后续又补了一份更稳妥的 UTF-8 HTML 可打开版本。

这一步说明，训练成果不仅被实现了，还被整理成了适合提交和展示的形式。

## 七、Kaggle 提交结果记录

在后续结果补充中，这次训练对应的 Kaggle 实际结果被记录为：

- **Score：0.77033**
- **Rank：8441**

这部分结果后来被补进了研究报告文件中，用于完成作业要求中的“提交结果与排名说明”。

## 八、这段训练过程的整体逻辑

如果按过程顺序概括，这一段训练可以总结为：

1. 明确 Titanic 作业需要完整交付，不只是跑模型；
2. 对基础视频流程进行优化；
3. 完成缺失值处理与特征工程设计；
4. 建立规范化预处理管道；
5. 比较多个模型，并用 5 折交叉验证评估；
6. 选出逻辑回归作为最佳模型；
7. 生成 Kaggle 提交文件；
8. 整理研究报告、Word 兼容版和可展示文档；
9. 补充 Kaggle 实际得分与排名结果。

## 九、可以直接引用的简短总结

前一段 Titanic 训练过程并不是单纯套用现成代码，而是围绕数据预处理、特征工程、模型比较和结果整理做了一次较完整的数据挖掘实践。训练中先优化了缺失值处理和特征构造，再通过 5 折交叉验证比较逻辑回归、随机森林和梯度提升模型，最终选择表现最优的逻辑回归生成 Kaggle 提交结果。随后又把整个训练过程整理成研究报告和 Word/HTML 可交付文档，并记录了最终的 Kaggle 得分 0.77033 和排名 8441。
